<%= form_with(model: @blog, local: true) do |form| %>
  <% if @blog.errors.any? %>
abbreviation
  <% end %>
  <div class="blog_title">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>
  <div class="blog_title">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>
  <%= form.submit %>
<% end %>
<%= link_to "return to the blog list screen", blogs_path %>
